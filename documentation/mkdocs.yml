site_name: PCM-Hub
theme:
  name: material
  palette:
    primary: indigo

plugins:
  - mkdocstrings

nav:
  - Home: index.md            
  - API completa: api.md
  - Database (ER): architettura.md

markdown_extensions:
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
